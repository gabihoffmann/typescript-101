// creating basic types

// primitives

// boolean (true | false)
let isBoolean: boolean
isBoolean = true

// string ( 'text', "text", `text`)
let isString: string
isString = `olá`

// number (int, float, hex, binary)
let isNumber: number
isNumber = 0xff0